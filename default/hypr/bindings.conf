# Basic keybinds
$mainMod = SUPER # Sets "Windows" key as main modifier
bind = $mainMod, return, exec, kitty
bind = $mainMod, E, exec, nautilus
bind = $mainMod, B, exec, firefox
bind = $mainMod, M, exit,
bind = $mainMod SHIFT, C, exec, hyprlock
bind = $mainMod SHIFT, X, exec, hyprpicker -a
bind = $mainMod, space, exec, pkill rofi || rofi -show drun -replace -i
# bind = $mainMod, space, exec, walker
bind = $mainMod, R, exec, pkill -SIGUSR1 waybar
bind = $mainMod SHIFT, N, exec, swaync-client -t -sw
bind = , PRINT, exec, hyprshot -m region --clipboard-only
bind = $mainMod, F, fullscreen, 0

# Binds for cool script shit
bind = $mainMod SHIFT, R, exec, refresh-app-daemons
bind = $mainMod SHIFT, space, exec, sokratos-next-theme
bind = $mainMod SHIFT, P, exec, sokratos-themes
bind = $mainMod, PRINT, exec, wf-start-recording
bind = $mainMod SHIFT, PRINT, exec, wf-stop-recording
bind = $mainMod SHIFT CTRL, PRINT, exec, sokratos-wf-recorder
bind = $mainMod SHIFT, return, exec, sokratos-floaterminal


# Binds for common websites
bind = $mainMod, A, exec, firefox --new-window "https://chatgpt.com/"
bind = $mainMod, G, exec, firefox --new-window "https://github.com/"
bind = $mainMod, C, exec, firefox --new-window "https://nmbu.instructure.com/"
bind = $mainMod SHIFT, B, exec, firefox --new-window "https://devhints.io/bash"

# Example special workspace (scratchpad)
# bind = $mainMod, W, togglespecialworkspace, magic
# bind = $mainMod SHIFT, W, movetoworkspace, special:magic1
