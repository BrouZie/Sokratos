#!/usr/bin/env bash

ROOT="$HOME/Documents/2ndBrain"

cd -- "$ROOT" || exit 1


# usr_choice=$(fd -t d --max-depth 1 . "$root" | fzf)

# usr_choice="$(
#   fd -t d --max-depth 1 . "$root" |
#   fzf --bind 'enter:accept-or-print-query'
# )"

# echo "$usr_choice"

read -rp "Filename: " TITLE
export TITLE
export DATE="123 424 monday"
export USR_CHOICE="PEEPEEEPOOOOPOOO"


TEMPLATE=$(envsubst < $ROOT/templates/Deep_Understanding_Loop.md)

printf '%s\n' "$TEMPLATE" >> "$ROOT/testttttttttttttt.md"
